import { deserializeTags, serializeTags } from "./src/tags";
// import { longTo8ByteArray, byteArrayToLong, shortTo2ByteArray, longTo16ByteArray, longTo32ByteArray, longToNByteArray } from "./src/utils";
import { toBuffer, encode } from "base64url";

(async function () {
  // const arBuf = toBuffer;
})();
